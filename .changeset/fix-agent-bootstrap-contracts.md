---
"@holocronlab/botruntime-adk": patch
"@holocronlab/brt": patch
---

Исправлены повторный production deploy старых agent targets с подтверждённо пустым plugin snapshot, сохранение явного `plugins` в staged definition, чтение `brt link --key-stdin` из перенаправленного stdin и точные версии BRT/runtime в новых agent-проектах.
