# Publish @holocronlab/brt to GitHub Packages. Trigger: tag brt-v* (points at any
# branch/sha) or manual dispatch. Consumers: npm install @holocronlab/brt (needs an
# @holocronlab:registry=npm.pkg.github.com npmrc + a token).
name: publish-brt-package

on:
  push:
    tags:
      - "brt-v*"
  workflow_dispatch:
    inputs:
      ref:
        description: "branch/sha to publish brt from"
        required: false
        default: fork-botpress-deps

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref || github.ref }}

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.3.14"

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://npm.pkg.github.com"
          scope: "@holocronlab"

      - name: Rewrite brt local file deps to published registry versions
        run: |
          node scripts/prepare-brt-publish.mjs --write
          mv packages/brt/bun.lock "$RUNNER_TEMP/brt.source.bun.lock"

      - name: Install and typecheck brt from registry dependencies
        working-directory: packages/brt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          bun install --no-save --ignore-scripts
          bun run typecheck

      - name: Publish brt to GitHub Packages (skip if this version is already published)
        working-directory: packages/brt
        run: |
          VER="$(node -p "require('./package.json').version")"
          if npm view "@holocronlab/brt@$VER" version >/dev/null 2>&1; then
            echo "@holocronlab/brt@$VER already published — skipping (immutable registry)."
          else
            npm publish
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
