{
  "name": "@botruntime/megaplan",
  "version": "0.2.10",
  "private": true,
  "type": "module",
  "description": "Megaplan CRM integration (Botpress-style) for the botruntime runtime-host. Replaces the Go CRM client.",
  "scripts": {
    "check:type": "tsc --noEmit",
    "build": "brt add -y && brt build",
    "test": "bun test"
  },
  "dependencies": {
    "@holocronlab/botruntime-sdk": "7.0.0",
    "@holocronlab/botruntime-zui": "2.3.1"
  },
  "devDependencies": {
    "@holocronlab/brt": "0.12.1",
    "@types/bun": "^1.3.14",
    "typescript": "^5.9.3"
  }
}
