# @holocronlab/botruntime-chat

Changelog starts 2026-07-18 (DEVLP-174) — earlier history: `git log -- packages/botruntime-chat`.

Chat API client used by the `brt` CLI — vendored dist of `@botpress/chat@0.5.5` (not a byte-exact
oracle rebuild, unlike `botruntime-client`). See README.md.

## 0.5.6 (current)

- feat(botruntime): api bootstrap + byte-exact client + chat (phase 2) — initial fork (f844fb5)
- feat: add hosted eval and chat platform support (ec9d6d4)
- Fix BRT and ADK platform contracts (#21)
