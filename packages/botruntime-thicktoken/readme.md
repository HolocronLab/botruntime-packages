# ThickToken

Tiktoken but thicker; a bundled tokenizer with added helper functions.

Self-contained fork published as `@holocronlab/botruntime-thicktoken`, with the WASM
tokenizer inlined into the bundle so consumers don't need any extra asset wiring.
