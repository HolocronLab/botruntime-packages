# botruntime-tunnel

Tunneling logic for both the client and server, used by the `brt` CLI to
expose a local dev server through a remote tunnel.

This package is a fork of `@bpinternal/tunnel` (MIT licensed), maintained
as `@holocronlab/botruntime-tunnel`.
